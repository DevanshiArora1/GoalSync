# Backend API origin (no trailing slash).
# Vite exposes only variables prefixed with VITE_ to the client.

# Local development
VITE_API_URL=http://127.0.0.1:5000

# Production (Render) — set this in your hosting provider's environment settings
# VITE_API_URL=https://your-goalsync-api.onrender.com
